# The expected-fail list — the executable half of wasi/COMPATIBILITY.md.
#
#   <suite>/<test>   <class>   <reason>
#
# Class is what COMPATIBILITY.md marks the row, and the three exist so that a
# defect is never quietly filed as a divergence:
#
#   divergence  the browser does not have the feature. A blank cell. Permanent
#               until the platform changes, and NOT a thing to fix.
#   gesture     real in a browser, but gated behind a user gesture no automated
#               test can supply. A game can reach it from a click; this cannot.
#   defect      the browser has it and we do not. Names its issue. THIS IS THE
#               TO-DO LIST, and it should only ever get shorter.
#
# wasi/corpus/run.sh fails if a test here PASSES: an unearned divergence is a
# lie, and a fixed defect must be struck from the list in the same change that
# fixes it. It also fails on a name here that the corpus does not have, so a
# renamed or deleted upstream test cannot hide a real failure behind a dead line.

# ── love.audio ────────────────────────────────────────────────────────────────
audio/RecordingDevice	divergence	a browser gives you the mic rate it gives you; setting it is not offered
audio/Source	divergence	Source:setFilter — per-source filters have no WebAudio equivalent inside LOVE's OpenAL-shaped model
audio/getActiveEffects	divergence	EFX effects, as above
audio/getEffect	divergence	EFX effects, as above
audio/setEffect	divergence	EFX effects, as above
audio/setPlaybackDevice	divergence	output-device selection is gated in a browser

# ── love.filesystem ───────────────────────────────────────────────────────────
filesystem/getRealDirectory	divergence	the store is virtual; there are no real directories to name
filesystem/mount	divergence	runtime archive mounting — D7 closed as deliberately not built (#48)
filesystem/mountCommonPath	divergence	userdesktop / userhome / appdocuments / userappdata — a page has no such paths
filesystem/unmount	divergence	the other half of D7 (#48)

# ── love.graphics ─────────────────────────────────────────────────────────────
graphics/Video	divergence	love.video is not linked; Theora in wasm is not the right path
graphics/newVideo	divergence	as above
# The four rasterisation near-misses. A different GL implementation rasterises
# edges differently: arc 3069/3072 pixels, circle 1022/1024, ellipse 1023/1024,
# setLineStyle 224/256. Desktop *behavioral* parity is the reference, not the
# bar (readme, fidelity standard §3) — so these are a divergence today. THE
# TOLERANCE IS AN OPEN DECISION: if it closes the other way these become
# defects, and this is the file that changes.
graphics/arc	divergence	rasterisation edge, 3069/3072 pixels match — tolerance decision open
graphics/circle	divergence	rasterisation edge, 1022/1024 — tolerance decision open
graphics/ellipse	divergence	rasterisation edge, 1023/1024 — tolerance decision open
graphics/setLineStyle	divergence	rasterisation edge, 224/256 — tolerance decision open

# ── love.joystick ─────────────────────────────────────────────────────────────
joystick/setGamepadMapping	divergence	no SDL controller database in a browser; the W3C standard mapping is fixed and implicit

# ── love.mouse ────────────────────────────────────────────────────────────────
mouse/setGrabbed	divergence	pointer confinement has no browser API at all

# ── love.sound ────────────────────────────────────────────────────────────────
sound/SoundData	divergence	getSample(0.001) — a D8 consequence, not a sound defect: luaL_checkinteger truncates under 5.1 and raises under 5.4

# ── love.system ───────────────────────────────────────────────────────────────
system/getOS	divergence	returns "Web" deliberately; the test asserts membership of a closed desktop-only list

# ── love.timer ────────────────────────────────────────────────────────────────
timer/getTime	divergence	measuring a sleep that did not happen — see below
timer/sleep	divergence	love::sleep is an honest no-op; a browser must not block its main thread

# ── love.window ───────────────────────────────────────────────────────────────
window/getPosition	divergence	a page cannot move its window
window/setPosition	divergence	a page cannot move its window
window/isMaximized	divergence	nothing to drive
window/isMinimized	divergence	nothing to drive
window/maximize	divergence	nothing to drive
window/minimize	divergence	nothing to drive
window/getIcon	divergence	a favicon belongs to the host document, not to the canvas the game owns
window/setIcon	divergence	storing the ImageData so getIcon round-trips would report an effect the browser never performed
window/getFullscreen	gesture	the Fullscreen API is real and requires a user gesture — a game can, a cold test cannot
window/setFullscreen	gesture	as above
window/isDisplaySleepEnabled	defect	Screen Wake Lock is real, async and permission-gated — implementable, currently a no-op
window/setDisplaySleepEnabled	defect	as above
